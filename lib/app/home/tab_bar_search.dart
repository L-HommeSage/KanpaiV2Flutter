import 'package:flutter/material.dart';

class TabBarSearch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text("ok2")));
  }
}
